# Where the Express API lives. Server-only: the browser calls /api/* on this
# app's own origin and next.config.ts rewrites it here, so the session cookie
# stays first-party. On Vercel, set this to the Railway service URL.
API_ORIGIN=http://localhost:4000
